{{- if .Values.sakhiutilityConfig.autoscaleEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.sakhiutilityConfig.minReplicas }}
  maxReplicas: {{ .Values.sakhiutilityConfig.maxReplicas }}
  metrics:
    {{- if .Values.sakhiutilityConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.sakhiutilityConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.sakhiutilityConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.sakhiutilityConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
