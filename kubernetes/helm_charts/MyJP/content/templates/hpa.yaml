{{- if .Values.contentConfig.autoscaleEnabled}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.contentConfig.minReplicas }}
  maxReplicas: {{ .Values.contentConfig.maxReplicas }}
  metrics:
    {{- if .Values.contentConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.contentConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.contentConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.contentConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
