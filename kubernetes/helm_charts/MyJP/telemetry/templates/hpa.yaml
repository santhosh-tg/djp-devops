{{- if .Values.telemetryConfig.autoscaleEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.telemetryConfig.minReplicas }}
  maxReplicas: {{ .Values.telemetryConfig.maxReplicas }}
  metrics:
    {{- if .Values.telemetryConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.telemetryConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.telemetryConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.telemetryConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
