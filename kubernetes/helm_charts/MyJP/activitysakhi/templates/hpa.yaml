{{- if .Values.activityConfig.autoscaleEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.activityConfig.minReplicas }}
  maxReplicas: {{ .Values.activityConfig.maxReplicas }}
  metrics:
    {{- if .Values.activityConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.activityConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.activityConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.activityConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
