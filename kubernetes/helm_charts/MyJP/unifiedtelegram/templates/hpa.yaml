{{- if .Values.unifiedtelegramConfig.autoscaleEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.unifiedtelegramConfig.minReplicas }}
  maxReplicas: {{ .Values.unifiedtelegramConfig.maxReplicas }}
  metrics:
    {{- if .Values.unifiedtelegramConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.unifiedtelegramConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.unifiedtelegramConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.unifiedtelegramConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
