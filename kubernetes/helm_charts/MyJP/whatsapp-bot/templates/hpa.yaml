{{- if .Values.whatsappConfig.autoscaleEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.whatsappConfig.minReplicas }}
  maxReplicas: {{ .Values.whatsappConfig.maxReplicas }}
  metrics:
    {{- if .Values.whatsappConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.whatsappConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.whatsappConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.whatsappConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
