{{- if .Values.adminutilConfig.autoscaleEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.adminutilConfig.minReplicas }}
  maxReplicas: {{ .Values.adminutilConfig.maxReplicas }}
  metrics:
    {{- if .Values.adminutilConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminutilConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.adminutilConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminutilConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}